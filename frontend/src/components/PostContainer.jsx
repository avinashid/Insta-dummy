import { useSelector } from "react-redux/es/hooks/useSelector";
import Post from "./Post";

const PostContainer = () => {
 
  // const post = posts.posts.map((e) => {
  //   <div>{e}</div>;
  // });
  // return <div>{!posts.isLoading && console.log("this will ",posts.isLoading) || post}</div>;
};

export default PostContainer;
